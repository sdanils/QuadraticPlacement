# Математическое описание алгоритмов квадратичного размещения

## Постановка задачи

### Входные данные

- **Граф** $G = (V, E)$ с $N$ вершинами и $M$ рёбрами
- **Множество фиксированных вершин** $V_F \subseteq V$ с известными координатами
- **Множество свободных вершин** $V_S = V \setminus V_F$ — координаты нужно найти
- **Веса рёбер** $w_{ij} = 1$ для всех $(i, j) \in E$

### Целевая функция

Минимизировать взвешенную сумму квадратов длин рёбер:

$$
\min_{x_1, y_1, \ldots, x_N, y_N} \sum_{(i,j) \in E} w_{ij} \left[ (x_i - x_j)^2 + (y_i - y_j)^2 \right]
$$

при ограничениях:
$$
x_i = \bar{x}_i, \quad y_i = \bar{y}_i, \quad \forall i \in V_F
$$

---

## Алгоритм 1: Базовый (матрица Лапласа)

### 1.1 Матрица Лапласа

Для графа $G$ матрица Лапласа $L \in \mathbb{R}^{N \times N}$ определяется как:

$$
L_{ij} = \begin{cases}
\deg(i) & \text{если } i = j \\
-1 & \text{если } (i,j) \in E \\
0 & \text{иначе}
\end{cases}
$$

где $\deg(i)$ — степень вершины $i$.

### 1.2 Система линейных уравнений

Задача минимизации сводится к решению двух независимых систем линейных уравнений:

$$
L \mathbf{x} = \mathbf{b}_x, \qquad L \mathbf{y} = \mathbf{b}_y
$$

где $\mathbf{x} = (x_1, \ldots, x_N)^T$, $\mathbf{y} = (y_1, \ldots, y_N)^T$.

### 1.3 Редукция системы

Разобьём индексы на свободные и фиксированные:

$$
\begin{bmatrix}
L_{SS} & L_{SF} \\
L_{FS} & L_{FF}
\end{bmatrix}
\begin{bmatrix}
\mathbf{x}_S \\
\mathbf{x}_F
\end{bmatrix}
=
\begin{bmatrix}
\mathbf{0} \\
\mathbf{x}_F
\end{bmatrix}
$$

Редуцированная система для свободных вершин:

$$
L_{SS} \mathbf{x}_S = -L_{SF} \mathbf{x}_F
$$

Аналогично для $y$-координат:

$$
L_{SS} \mathbf{y}_S = -L_{SF} \mathbf{y}_F
$$

### 1.4 Метод сопряжённых градиентов

Для решения редуцированной системы используется итерационный метод сопряжённых градиентов:

**Инициализация:**
$$
\mathbf{r}_0 = \mathbf{b} - L_{SS}\mathbf{x}_0, \quad \mathbf{p}_0 = \mathbf{r}_0
$$

**Итерация $k$:**
$$
\alpha_k = \frac{\mathbf{r}_k^T \mathbf{r}_k}{\mathbf{p}_k^T L_{SS} \mathbf{p}_k}
$$

$$
\mathbf{x}_{k+1} = \mathbf{x}_k + \alpha_k \mathbf{p}_k
$$

$$
\mathbf{r}_{k+1} = \mathbf{r}_k - \alpha_k L_{SS} \mathbf{p}_k
$$

$$
\beta_k = \frac{\mathbf{r}_{k+1}^T \mathbf{r}_{k+1}}{\mathbf{r}_k^T \mathbf{r}_k}
$$

$$
\mathbf{p}_{k+1} = \mathbf{r}_{k+1} + \beta_k \mathbf{p}_k
$$

**Критерий остановки:** $\|\mathbf{r}_k\| < \varepsilon = 10^{-10}$

### 1.5 Параметры алгоритма

| Параметр | Значение |
|----------|----------|
| Толерантность $\varepsilon$ | $10^{-10}$ |
| Максимальное число итераций | 10 000 |
| Формат матрицы | CSR (разреженная) |

### 1.6 Вычислительная сложность

- Построение матрицы Лапласа: $O(N + M)$
- Одна итерация СГ: $O(N + M)$ (эффективно для разреженных матриц)
- Общая сложность: $O((N + M) \cdot \sqrt{\kappa} \cdot \log(1/\varepsilon))$

где $\kappa$ — число обусловленности матрицы $L_{SS}$.

---

## Алгоритм 2: Эвристический (силовой метод)

### 2.1 Физическая интерпретация

Система моделируется как набор частиц (вершин) соединённых пружинами (рёбра).

### 2.2 Сила притяжения (пружины Хука)

Для каждого ребра $(i, j) \in E$:

$$
\vec{F}_{attraction}(i, j) = k \cdot (d_{ij} - d_0) \cdot \frac{\vec{r}_j - \vec{r}_i}{d_{ij}}
$$

где:
- $k$ — коэффициент жёсткости пружины
- $d_{ij} = \|\vec{r}_i - \vec{r}_j\|$ — текущее расстояние
- $d_0$ — идеальная длина пружины
- $\vec{r}_i = (x_i, y_i)$ — позиция вершины

### 2.3 Сила отталкивания (кулоновское)

Для каждой пары вершин $(i, j)$:

$$
\vec{F}_{repulsion}(i, j) = \frac{C}{d_{ij}^2} \cdot \frac{\vec{r}_j - \vec{r}_i}{d_{ij}}
$$

где $C$ — константа отталкивания.

### 2.4 Результирующая сила

Для каждой свободной вершины $i \in V_S$:

$$
\vec{F}_i = \sum_{j: (i,j) \in E} \vec{F}_{attraction}(i, j) - \sum_{j \neq i} \vec{F}_{repulsion}(i, j)
$$

### 2.5 Обновление позиций

С имитацией отжига (cooling):

$$
\vec{r}_i^{(t+1)} = \vec{r}_i^{(t)} + \Delta \vec{r}_i
$$

где смещение ограничено температурой:

$$
\Delta \vec{r}_i = \min\left(\frac{T}{\max \|\vec{F}\|} \cdot \vec{F}_i,\; T \cdot \frac{\vec{F}_i}{\|\vec{F}_i\|}\right)
$$

### 2.6 Охлаждение (Simulated Annealing)

$$
T_{t+1} = \alpha \cdot T_t
$$

где $\alpha \in (0, 1)$ — коэффициент охлаждения.

### 2.7 Энергия системы

$$
E = \sum_{(i,j) \in E} \frac{k}{2} (d_{ij} - d_0)^2
$$

### 2.8 Критерий остановки

- Максимальное число итераций (адаптивное):
  - $N < 100$: 500 итераций
  - $100 \leq N < 1000$: 200 итераций
  - $N \geq 1000$: 100 итераций
- Или стагнация: $|E_t - E_{t-1}| < \varepsilon_{stag}$ в течение 10 итераций

### 2.9 Параметры алгоритма

| Параметр | Обозначение | Значение |
|----------|-------------|----------|
| Коэффициент пружины | $k$ | 1.0 |
| Идеальная длина | $d_0$ | 50.0 |
| Константа отталкивания | $C$ | 1000.0 |
| Начальная температура | $T_0$ | 100.0 |
| Коэффициент охлаждения | $\alpha$ | 0.95 |
| Порог стагнации | $\varepsilon_{stag}$ | $10^{-6} \cdot \max(1, N/1000)$ |

### 2.10 Вычислительная сложность

Одна итерация: $O(M + N^2)$

Общая сложность: $O(K \cdot (M + N^2))$, где $K$ — число итераций.

**Примечание:** Силы отталкивания между всеми парами вершин дают квадратичную сложность по $N$.

---

## Сравнение алгоритмов

| Характеристика | Базовый (Лаплас) | Эвристический (силы) |
|----------------|------------------|----------------------|
| Тип решения | Точное (оптимальное) | Приближённое |
| Целевая функция | Квадратичная | Энергетическая |
| Сходимость | $O(\sqrt{\kappa} \cdot \log \varepsilon)$ | Зависит от параметров |
| Сложность | $O(K_{CG} \cdot (N + M))$ | $O(K_{force} \cdot (N^2 + M))$ |
| Масштабируемость | Отличная (>10K вершин) | Плохая (~1K вершин) |
| Гарантии | Глобальный минимум | Локальные оптимумы |

---

## Метрики качества

Для оценки результата вычисляются:

1. **Суммарная длина:**
   $$L_{total} = \sum_{(i,j) \in E} d_{ij}$$

2. **Максимальная длина:**
   $$L_{max} = \max_{(i,j) \in E} d_{ij}$$

3. **Минимальная длина:**
   $$L_{min} = \min_{(i,j) \in E} d_{ij}$$

4. **Средняя длина:**
   $$L_{avg} = \frac{L_{total}}{M}$$

где $d_{ij} = \sqrt{(x_i - x_j)^2 + (y_i - y_j)^2}$ — евклидово расстояние.

---

## Рекомендации по выбору алгоритма

- **Базовый алгоритм:** используйте всегда, когда требуется качественное решение. Он даёт математически оптимальный результат для квадратичной целевой функции.

- **Эвристический алгоритм:** может использоваться для визуализации небольших графов или когда нужна интерпретируемость процесса (можно наблюдать "движение" вершин).
