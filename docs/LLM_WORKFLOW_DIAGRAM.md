# Процесс работы с LLM при разработке исследовательского ПО

> Диаграмма описывает оптимальный процесс взаимодействия исследователя с LLM
> (Claude Code, GPT и др.) для написания программ научных вычислений.
>
> Пример основан на проекте **Quadratic Placement Solver**, но подход универсален.

---

## Общая схема процесса

```mermaid
flowchart TD
    START([Начало: исследовательская задача]) --> P0

    subgraph P0["Этап 0: Фундамент"]
        P0A["📄 Создать CLAUDE.md\nкоманды сборки, тестов,\nстиль кода, обозначения"]
        P0B["📁 Инициализировать структуру\nпроекта и репозиторий"]
        P0A --> P0B
    end

    P0 --> P1

    subgraph P1["Этап 1: Спецификация"]
        P1A["📝 Математическая постановка\nзадачи (формулы, обозначения)"]
        P1B["📐 Описание алгоритмов\nна математическом уровне"]
        P1C["📊 Анализ сложности\nи граничных условий"]
        P1A --> P1B --> P1C
    end

    P1 --> P2

    subgraph P2["Этап 2: Планирование"]
        P2A["🔍 LLM: анализ спецификации\nв режиме планирования"]
        P2B["📋 LLM: создать план реализации\n(файлы, структуры данных,\nпотоки вычислений)"]
        P2C["✅ Исследователь: ревью\nи корректировка плана"]
        P2A --> P2B --> P2C
        P2C -- правки --> P2A
    end

    P2 --> P3

    subgraph P3["Этап 3: Проектирование тестов"]
        P3A["🧪 Аналитические тесты\n(известные решения\nдля малых случаев)"]
        P3B["🧪 Тесты свойств\n(инварианты: симметрия,\nположительная определённость)"]
        P3C["🧪 Интеграционные тесты\n(полный конвейер)"]
        P3A --> P3B --> P3C
    end

    P3 --> P4

    subgraph P4["Этап 4: Итеративная реализация"]
        direction TB
        P4A["🔨 LLM: реализовать\nодин модуль"]
        P4B["⚡ LLM: запустить тесты\nи исправить ошибки"]
        P4C{"Все тесты\nпроходят?"}
        P4D["💾 Коммит рабочего\nинкремента"]
        P4E{"Все модули\nреализованы?"}

        P4A --> P4B --> P4C
        P4C -- Нет --> P4A
        P4C -- Да --> P4D --> P4E
        P4E -- Нет --> P4A
    end

    P4E -- Да --> P5

    subgraph P5["Этап 5: Валидация"]
        P5A["🔬 Проверка на\nаналитических решениях"]
        P5B["📊 Сравнение алгоритмов\nна общих входных данных"]
        P5C["📈 Нагрузочное тестирование\n(масштабируемость)"]
        P5D["⚠️ Документирование\nкраевых эффектов\n(NaN, расходимость)"]
        P5A --> P5B --> P5C --> P5D
    end

    P5 --> P6

    subgraph P6["Этап 6: Ревью и рефакторинг"]
        P6A["🔎 LLM: ревью кода\n(корректность, краевые случаи)"]
        P6B["♻️ Рефакторинг:\nустранение дублирования,\nоптимизация"]
        P6C["📖 Обновление документации\nи CLAUDE.md"]
        P6A --> P6B --> P6C
    end

    P6 --> DONE([✅ Готово: проверенная\nисследовательская программа])
```

---

## Детальная схема итеративного цикла реализации (Этап 4)

```mermaid
sequenceDiagram
    participant R as 🔬 Исследователь
    participant L as 🤖 LLM
    participant T as 🧪 Тесты
    participant G as 📦 Git

    R->>L: Промпт: «Реализуй модуль X по плану.<br/>Напиши тесты, запусти, исправь ошибки»

    loop Цикл модуля
        L->>L: Анализирует план и спецификацию
        L->>L: Пишет код модуля
        L->>L: Пишет тесты к модулю
        L->>T: Запускает тесты

        alt Тесты падают
            T-->>L: ❌ Failures с деталями
            L->>L: Анализирует ошибки
            L->>L: Исправляет код
            L->>T: Перезапускает тесты
        end

        T-->>L: ✅ Все тесты пройдены
        L-->>R: «Модуль X реализован.<br/>N тестов пройдены»
        R->>G: Коммит
    end

    Note over R,G: Цикл повторяется для каждого модуля:<br/>Core → Data → Algorithms → Visualization → Reporting → CLI
```

---

## Роль LLM на каждом этапе

```mermaid
mindmap
  root((Роль LLM))
    Этап 0: Фундамент
      Генерация начального CLAUDE.md
      Создание структуры проекта
      Настройка CI/CD и тестов
    Этап 1: Спецификация
      Помощь в формализации математики
      Проверка полноты постановки
      Генерация примеров и контрпримеров
    Этап 2: Планирование
      Анализ спецификации
      Проектирование архитектуры
      Выбор структур данных
      Оценка сложности реализации
    Этап 3: Тесты
      Генерация тестов по спецификации
      Аналитические тесты
      Тесты свойств и инвариантов
    Этап 4: Реализация
      Написание кода по плану
      Самопроверка через тесты
      Итеративное исправление
    Этап 5: Валидация
      Сравнение результатов алгоритмов
      Анализ расходимостей
      Документирование аномалий
    Этап 6: Ревью
      Код-ревью на корректность
      Поиск оптимизаций
      Генерация документации
```

---

## Пример промптов для проекта Quadratic Placement

```mermaid
flowchart LR
    subgraph Промпт["Шаблон промпта для каждого модуля"]
        direction TB
        A["📋 Контекст: ссылка на CLAUDE.md"]
        B["📐 Спецификация: какая задача решается"]
        C["📦 План: какой модуль реализуем"]
        D["🧪 Тесты: какие проверки нужны"]
        E["✅ Критерий: когда считать готовым"]
        A --> B --> C --> D --> E
    end
```

### Пример конкретного промпта

```
Реализуй модуль QuadraticPlacement.Core по плану.

Контекст: см. CLAUDE.md (команда сборки: dotnet build)
Спецификация: см. docs/MATHEMATICAL_DESCRIPTION.md

Необходимо:
1. Создать модели данных: Graph, Edge, FixedVertex, Metrics
2. Создать интерфейс IPlacementSolver
3. Реализовать SparseMatrixCSR для разреженных матриц

Тесты:
- Единичная матрица в CSR формате
- Симметричная матрица 3×3
- Пустая матрица

Запусти тесты (dotnet test) и исправь ошибки.
Не переходи к следующему модулю, пока все тесты не пройдут.
```

---

## Жизненный цикл итерации исследования

```mermaid
stateDiagram-v2
    [*] --> Идея

    Идея --> Формализация: Исследователь формулирует задачу
    Формализация --> LLM_Спецификация: Передача математической постановки

    state LLM_Спецификация {
        [*] --> Анализ_постановки
        Анализ_постановки --> Создание_плана
        Создание_плана --> Ревью_плана
        Ревью_плана --> Анализ_постановки: Правки
        Ревью_плана --> [*]: Одобрено
    }

    LLM_Спецификация --> Реализация_итерации

    state Реализация_итерации {
        [*] --> Кодинг
        Кодинг --> Тестирование
        Тестирование --> Кодинг: Ошибка
        Тестирование --> Коммит: Успех
        Коммит --> [*]
    }

    Реализация_итерации --> Валидация
    Валидация --> Анализ_результатов

    Анализ_результатов --> Идея: Новая гипотеза
    Анализ_результатов --> Публикация: Результаты корректны

    state Публикация {
        [*] --> Финальное_ревью
        Финальное_ревью --> Очистка_кода
        Очистка_кода --> Документация
        Документация --> [*]
    }

    Публикация --> [*]
```

---

## Ключевые принципы

| # | Принцип | Почему это важно |
|---|---------|-----------------|
| 1 | **Сначала спецификация, потом код** | LLM точнее реализует формальную математику, чем словесное описание |
| 2 | **Отделять планирование от реализации** | Позволяет скорректировать архитектуру до написания кода |
| 3 | **TDD: тесты до реализации** | Тесты определяют критерий готовности; LLM итерирует до их прохождения |
| 4 | **Давать LLM возможность самопроверки** | LLM работает значительно лучше, когда может запускать тесты и видеть результат |
| 5 | **Малые итерации с коммитами** | Ошибки дешевле исправлять сразу; всегда есть точка отката |
| 6 | **Валидация на известных решениях** | Единственный способ убедиться в корректности научного кода |
| 7 | **Исследователь проверяет математику** | LLM реализует алгоритм, но человек должен верифицировать постановку задачи |
| 8 | **Обновлять CLAUDE.md** | Накопленный контекст помогает LLM в следующих сессиях |

---

## Инструменты и форматы

```mermaid
flowchart TB
    subgraph Инструменты["Рекомендуемый стек инструментов"]
        direction LR
        LLM["🤖 LLM<br/>(Claude Code, Cursor,<br/>GitHub Copilot)"]
        VCS["📦 Git + Worktrees<br/>(параллельные эксперименты)"]
        CI["🔄 CI/CD<br/>(автозапуск тестов)"]
        DOC["📖 Mermaid + Markdown<br/>(документация и диаграммы)"]
        TEST["🧪 xUnit / pytest<br/>(тестирование)"]
    end

    subgraph Файлы["Ключевые файлы проекта"]
        CLAUDE_MD["CLAUDE.md — контекст для LLM"]
        MATH["docs/MATHEMATICAL_DESCRIPTION.md"]
        TESTS["docs/TEST_SUMMARY.md"]
        PLAN["docs/ — планы и диаграммы"]
    end

    Инструменты -.-> Файлы
```

---

> **Как читать эту диаграмму:** Откройте файл в VS Code с расширением *Markdown Preview Mermaid Support*
> или на [mermaid.live](https://mermaid.live) для рендеринга.
